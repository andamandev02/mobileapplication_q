name: app_apqueue
description: A Flutter app for Apqueue
version: 1.0.0+1

environment:
  sdk: ">=2.18.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  # ใส่ dependencies อื่นๆ ที่คุณต้องการสำหรับ flavor นี้

flutter:
  uses-material-design: true
  assets:
    - assets/logo/
    - assets/icon/
    - assets/fonts/NotoSansSC-Regular.ttf
